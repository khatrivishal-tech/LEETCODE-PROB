class Solution {
    public int removeDuplicates(int[] arr) {
       int i=0;
       int j;
       for(j=1;j<arr.length;j++){
            if(arr[i]==arr[j]){

            }

            else
            {
                i++;
                arr[i]=arr[j];
            }
       }
       return i+1;
    }
}
